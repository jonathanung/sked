/**
 * @returns An array with all names of days in a week
 */
export default function getAllDayNames() {
    const dayArr = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]
    return dayArr;
}